
@model SDP.Models.Cart
@{
    SDP.Interfaces.ICustomer cutomer = SDP.Services.ViewService.getCustomerFromList((string)ViewData["Id"]);
    decimal total = 0;
}

<section id="section-banner">
    <div class="container">
        <h2>Shopping cart</h2>
        <p>You have selected the following items.</p>
    </div>
</section>
<section>
    <div class="container">
        <div class="cart-contents">
            <ul>
                @if (cutomer != null)
                {
                    foreach (var product in cutomer.cart.getCartList())
                    {
                        total = total + (decimal)product.Key.price* (decimal)product.Value;
                        decimal subTotal = product.Key.price * product.Value;
                        <li class="cart-item">
                            <img src="~/imgs/@product.Key.imgUrl" alt="@product.Key.title" />
                            <div class="cart-item-desc">
                                <h4 class="desc">@product.Key.title - @product.Key.brand.title</h4>
                                <p>@product.Key.price LKR</p>
                            </div>
                            <div class="cart-item-qty">
                                <h5 class="desc">Quantity</h5>
                                <p>@product.Value</p>
                            </div>
                            <div class="cart-item-total">
                                <h5 class="desc">Item Total</h5>
                                <p>Rs. @subTotal</p>
                            </div>
                        </li>
                    }
                }
            </ul>
            <div class="cart-total">
                <div class="cart-total-box">
                    <h4>Order Total</h4>
                    <p>Rs. @total</p>
                </div>
                <button type="button" id="cart-btn-checkout" class="sdp-style">Proceed to Checkout</button>
                <small><a href="#">Continue Shopping</a></small>
            </div>
        </div>
    </div>
</section>